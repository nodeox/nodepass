version: "2.0"

node:
  id: "controller-1"
  type: "ingress"

controller:
  enabled: true
  address: "0.0.0.0:8443"

observability:
  metrics:
    enabled: true
    listen: "0.0.0.0:9090"
  logging:
    level: "info"
    format: "json"
